Exam: about navigation and connecting the tabs using routerLink and calling the attributes using {{}} 
User profile : the user sees the age name enrolment status and email address
register: the user is asked to input some information to fill out and is requested to fill the missing forms
